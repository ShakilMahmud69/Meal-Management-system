{
  "name": "meal-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "bootstrap": "npm install --workspaces",
    "dev": "cross-env VITE_BACKEND_URL=http://localhost:5001 PORT=5001 concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "backend": "npm --workspace backend run dev",
    "frontend": "npm --workspace frontend run dev"
  }
}
